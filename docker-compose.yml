services:
  mongo:
    image: mongo
    ports:
      - "27017:27017"

  external-api:
    build:
      context: .
      target: final
    ports:
      - "5190:8080"
    environment:
      - "ConnectionStrings__mongo=mongodb://mongo/"
